// Set request default parameters for openai, add tools here

const completionOptions = {
  // frequency_penalty: 0,
  // logit_bias: null,
  // logprobs: false,
  // top_logprobs:null,
  // n:1,
  // response_format: null,
  // presence_penalty: 0,
  // seed: null,
  // service_tier: null
  // stop: null,
  // temperature: 1,
  // top_p: 1,
  // tools: null,
  // tool_choice: null,
  // parallel_tool_calls: true,
  // user: null,
  max_tokens: 1000,
}

module.exports = {
  completionOptions
}